=LAMBDA(
  pWtKg,
  pHtCm,
  pAgeYears,
  pIsMale,
  LET(
    _functionName,
    N(
      "NEEDS REVISION, function name: fn_BEE_METRIC"
    ),
    _result,
    10 * pWtKg + 6.25 * pHtCm - 5 * pAgeYears + IF(
      pIsMale,
      5,
      - 161
    ),
    _result
  )
)