=LAMBDA(
  pWtLbs,
  pHtInches,
  LET(
    _functionName,
    N(
      "NEEDS REVISION, fn_BMI_USC"
    ),
    _wtKg,
    pWtLbs / c_POUNDS_PER_KILOGRAM,
    _htM,
    pHtInches / c_INCHES_PER_METER,
    _result,
    fn_BMI_METRIC(
      _wtKg,
      _htM
    ),
    _result
  )
)